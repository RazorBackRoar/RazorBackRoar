## 2024-08-21 - Explicit Context for Image Badges

**Learning:** Image badges in README files lack explicit context for screen readers when they only contain the technology name (e.g., "Python"), which can be confusing without the context of it being a badge.
**Action:** Always include explicit context (such as "technology badge") in the `alt` text of image badges to ensure accessibility for screen readers.
